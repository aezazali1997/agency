import { createUseStyles } from "react-jss";
export const styles = createUseStyles({
	sub: {
		overflow: "hidden",
		background: "#ededed",
		padding: "12rem 0",
	},
	subTxt: {
		margin: "2rem 0 0 3.5rem",
		fontSize: "1.5rem",
		width: "40%",
		color: "#555",
		fontWeight: 500,
	},
});
